{% block body %}
    <div class="flex items-center justify-center min-h-screen bg-gray-100">
        <div class="w-full max-w-md p-6 bg-white shadow-lg rounded-lg text-center">
            <h1 class="text-2xl font-bold text-gray-700 mb-4">Password Reset Email Sent</h1>
            <p class="text-gray-600 mb-4">
                If an account matching your email exists, then an email was just sent that contains a link you can use to reset your password.
                This link will expire in <span class="font-semibold text-gray-700">
                    {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}
                </span>.
            </p>
            <p class="text-gray-600">
                If you don’t receive an email, please check your spam folder or
                <a href="{{ path('app_forgot_password_request') }}" class="text-blue-500 hover:underline font-medium">
                    try again
                </a>.
            </p>
        </div>
    </div>
{% endblock %}